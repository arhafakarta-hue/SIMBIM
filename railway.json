{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev --optimize-autoloader && mkdir -p /app/database && touch /app/database/database.sqlite && chmod -R 775 /app/database"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "mkdir -p /app/database && touch /app/database/database.sqlite && chmod 775 /app/database/database.sqlite && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
